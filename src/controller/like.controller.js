import { asyncHandler } from "../utils/asyncHandler.js"

const likeComment = asyncHandler((req, res, next)=>{
    
})

const unlikeComment = asyncHandler((req, res, next)=>{
    
})

const likeVideo = asyncHandler((req, res, next)=>{
    
})

const unlikeVideo = asyncHandler((req, res, next)=>{
    
})

export {
    likeComment,
    unlikeComment,
    likeVideo,
    unlikeVideo,
}